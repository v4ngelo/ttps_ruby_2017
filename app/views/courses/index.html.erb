<p id="notice"><%= notice %></p>

<h3 class="min-margin"> Cursadas </h3>
<hr/>
<div class="min-margin">
  <%= button_to "Nueva Cursada", new_course_path, :method => :get, class: "btn btn-primary min-margin right"%>
</div>

<table class="table table-striped table-sm table-bordered">
  <thead class="thead-dark">
    <tr>
      <th>#</th>
      <th>Año de Cursada</th>
      <th colspan="2"></th>
    </tr>
  </thead>

  <% @courses.each do |course| %>
    <tr>
      <td><%= course.id %></td>
      <td><%= course.anio %></td>
      <td><%= button_to "Editar", edit_course_path(course.id), :method => :get, class: "btn btn-sm btn-info" %>
      <td><%= link_to 'Borrar', course_path(course), remote: true, :method => :delete,
                      data: {confirm: "Borrar Cursada: #{course.id} "}, :class => 'btn btn-sm btn-danger' %></td>
    </tr>
  <% end %>
</table>